# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Note: this project targets SDK 54, not the latest SDK — Expo Go on the App Store currently only supports SDK 54, so don't bump the `expo` package version without checking Expo Go compatibility first.
